# The summarizer is no longer a standalone agent in the LangGraph workflow.
# Each specialized node (car_node, appointment_node) summarizes its own output directly.
# This module is kept as a placeholder for future re-introduction of a dedicated
# summarizer step if the workflow becomes more complex.
